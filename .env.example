TELEGRAM_BOT_TOKEN=你的telegram_bot_token

# rclone 远端名（需要在 rclone.conf 中存在）
RCLONE_REMOTE=onedrive

# 可选：上传目录，默认 TelegramVideos
ONEDRIVE_TARGET_DIR=TelegramVideos

# 可选：只允许某个聊天ID使用，不填则不限制
# ALLOWED_CHAT_ID=123456789

# 可选：rclone 配置文件路径（Docker Compose 默认会覆盖为 /data/rclone.conf）
# RCLONE_CONFIG_FILE=/data/rclone.conf
